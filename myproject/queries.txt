# 1.
from main.models import Category, Product, Review

# 2. 
cat1 = Category.objects.create(name="Электроника")
cat2 = Category.objects.create(name="Киім")
cat3 = Category.objects.create(name="Кітаптар")
cat4 = Category.objects.create(name="Спорт")
cat5 = Category.objects.create(name="Ойыншықтар")

# Жазбалар 
Product.objects.create(category=cat1, title="MacBook Air M2", price=650000, stock=5)
Product.objects.create(category=cat3, title="Django for Beginners", price=9000, stock=40)
Product.objects.create(category=cat5, title="Puzzle 1000", price=4500, stock=60)

# 3. gte және icontains
complex_filter = Product.objects.filter(price__gte=50000, title__icontains="Mac")
print(complex_filter)

# 4. order_by
sorted_list = Product.objects.all().order_by('-price')
for p in sorted_list:
    print(f"{p.title}: {p.price}")

# 5. update
# Стокта 10-нан аз қалған тауарлардың бағасын өзгерту
Product.objects.filter(stock__lt=10).update(price=100000)

# 6. delete
# Бағасы 5000-нан төмен тауарларды өшіру
Product.objects.filter(price__lt=5000)